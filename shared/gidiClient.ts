/* ───────────────────────── src/service/gidiClient.ts ───────────────────────── */

export interface GidiConfig {
  merchantId: string;
  subMerchantId: string;
  credentialKey: string;
  baseUrl: string;
}
